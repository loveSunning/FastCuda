# Prompt: Benchmark CUDA Kernel

Benchmark a CUDA kernel with reproducible metadata.

Required output:

- benchmark command and config
- environment snapshot path
- result artifact path
- summary of latency and throughput
- comparison against baseline if available
